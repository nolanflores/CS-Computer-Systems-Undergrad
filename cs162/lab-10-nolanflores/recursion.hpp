#ifndef RECURSION_HPP
#define RECURSION_HPP

/*
 * Function: no_zero_strings
 * Description: Returns the number of distinct strings containing a total of
 * 		n characters consisting only of 0s and 1s with no consecutive 0s.
 * Parameters:
 * 		n (int): Number of characters in bitstring.
 * Returns (int): Number of distinct strings containing a total of n
 * 		characters consisting only of 0s and 1s with no consecutive 0s.
 */
int no_zero_strings(int n);

#endif
